# Инструкция по публикации media-sessions на crates.io

## ✅ Чек-лист перед публикацией

### 1. Подготовка окружения

```bash
# Установите Rust (если не установлен)
# https://rustup.rs/

# Убедитесь что версия Rust >= 1.80
rustc --version

# Установите cargo-edit для управления зависимостями (опционально)
cargo install cargo-edit
```

### 2. Логин на crates.io

```bash
# Войдите в свой аккаунт crates.io
cargo login

# Вам понадобится API токен из https://crates.io/settings/tokens
# Вставьте токен когда cargo запросит
```

### 3. Локальная проверка

```bash
# Перейдите в директорию проекта
cd path/to/media-sessions

# Отформатируйте код
cargo fmt --all

# Запустите clippy (исправьте все предупреждения)
cargo clippy --all-targets -- -D warnings

# Запустите тесты
cargo test --all-features

# Проверьте сборку для всех платформ
cargo check --target x86_64-pc-windows-msvc
cargo check --target x86_64-apple-darwin
cargo check --target x86_64-unknown-linux-gnu

# Сгенерируйте документацию и проверьте warnings
cargo doc --all-features --no-deps
```

### 4. Проверка package

```bash
# Проверьте что будет включено в пакет
cargo package --list

# Протестируйте сборку из пакета
cargo package --allow-dirty
```

### 5. Публикация

```bash
# Сухой запуск (покажет что будет опубликовано)
cargo publish --dry-run

# Публикация на crates.io
cargo publish
```

## 📝 Версионирование

Проект использует [Semantic Versioning](https://semver.org/):

- **MAJOR.MINOR.PATCH** (например, 0.2.0)
- **MAJOR** — ломающие изменения
- **MINOR** — новые функции (обратно совместимые)
- **PATCH** — исправления багов

### Обновление версии

```bash
# Вручную отредактируйте Cargo.toml:
# version = "0.2.1"

# Или используйте cargo-edit:
cargo set-version 0.2.1
```

### Обновление CHANGELOG.md

Перед публикацией обновите `CHANGELOG.md`:

```markdown
## [0.2.1] - 2026-02-22

### Added
- Новая функциональность

### Fixed
- Исправление бага

### Changed
- Изменения в API
```

## 🔧 Публикация обновлений

### Patch релиз (исправления багов)

```bash
# Обновите версию в Cargo.toml
# 0.2.0 -> 0.2.1

cargo publish
```

### Minor релиз (новые функции)

```bash
# Обновите версию в Cargo.toml
# 0.2.0 -> 0.3.0

cargo publish
```

### Major релиз (ломающие изменения)

```bash
# Обновите версию в Cargo.toml
# 0.2.0 -> 1.0.0

cargo publish
```

## 🚀 После публикации

### 1. Проверьте публикацию

Откройте https://crates.io/crates/media-sessions

### 2. Создайте релиз на GitHub

```bash
# Создайте тег
git tag -a v0.2.0 -m "Release version 0.2.0"

# Отправьте тег
git push origin v0.2.0

# Создайте релиз на GitHub с release notes
```

### 3. Обновите документацию

```bash
# docs.rs автоматически сгенерирует документацию
# Проверьте на https://docs.rs/media-sessions/latest/media_sessions/
```

## ⚠️ Важные замечания

### Feature flags

При публикации убедитесь что:
- `default` фичи работают корректно
- Каждая платформенная фича компилируется отдельно

### MSRV (Minimum Supported Rust Version)

В `Cargo.toml` указано:
```toml
rust-version = "1.80"
```

Не используйте функции из более новых версий Rust.

### Лицензия

Проект dual-licensed (MIT OR Apache-2.0). Убедитесь что:
- `LICENSE-MIT` и `LICENSE-APACHE` файлы присутствуют
- `Cargo.toml` содержит правильную лицензию

## 🔍 Troubleshooting

### Ошибка: "crate name must be lowercase"

```toml
# В Cargo.toml название должно быть в lowercase
name = "media-sessions"
```

### Ошибка: "already uploaded"

```bash
# Если версия уже существует, увеличьте версию
# 0.2.0 -> 0.2.1
```

### Ошибка: "missing license file"

Убедитесь что файлы лицензий существуют:
```
LICENSE-MIT
LICENSE-APACHE
```

### Ошибка компиляции на другой платформе

```bash
# Используйте cross-compilation для проверки
cargo check --target x86_64-unknown-linux-gnu
```

## 📚 Полезные ссылки

- [crates.io](https://crates.io/)
- [Cargo documentation](https://doc.rust-lang.org/cargo/)
- [Publishing guide](https://doc.rust-lang.org/cargo/reference/publishing.html)
- [Semantic Versioning](https://semver.org/)
- [Keep a Changelog](https://keepachangelog.com/)

---

**Автор:** krosov_ok
**Telegram:** [@krosov_ok](https://t.me/krosov_ok)
**GitHub:** [@krosovok52](https://github.com/krosovok52)
