# 🚀 Как собрать и опубликовать на GitHub

## ⚡ Быстрая команда

### Windows (PowerShell)
```powershell
.\setup-and-publish.ps1
```

### Linux/macOS (Bash)
```bash
./setup-and-publish.sh
```

---

## 📋 Полная инструкция

### 1. Установка зависимостей

```bash
# Rust (если не установлен)
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# mdBook
cargo install mdbook

# Git (если не установлен)
# Windows: https://git-scm.com/
# Linux: sudo apt install git
# macOS: brew install git
```

### 2. Подготовка репозитория

```bash
# Перейти в директорию проекта
cd c:\Users\rykov\OneDrive\Рабочий стол\MediaSession

# Проверить статус
git status

# Закоммитить изменения
git add .
git commit -m "docs: полная документация"

# Отправить на GitHub
git push origin main
```

### 3. Публикация документации

#### Вариант 1: Автоматически (GitHub Actions)

Просто запушьте изменения — документация опубликуется автоматически через 2-3 минуты.

```bash
git push origin main
```

#### Вариант 2: Скриптом

```powershell
# Windows
.\setup-and-publish.ps1
```

```bash
# Linux/macOS
./setup-and-publish.sh
```

#### Вариант 3: Вручную

```bash
# Собрать документацию
cd docs
mdbook build

# Опубликовать
git checkout --orphan gh-pages
git reset --hard
cp -r book/* .
touch .nojekyll
git add .
git commit -m "docs: publish"
git push origin gh-pages --force
git checkout main
```

### 4. Настройка GitHub Pages

1. Откройте https://github.com/krosovok52/media-sessions/settings/pages
2. **Source**: Deploy from a branch
3. **Branch**: gh-pages → **Save**

### 5. Проверка

Откройте в браузере:
```
https://krosovok52.github.io/media-sessions/
```

---

## 📁 Созданные файлы

| Файл | Назначение |
|------|------------|
| `setup-and-publish.ps1` | Публикация (Windows) |
| `setup-and-publish.sh` | Публикация (Linux/macOS) |
| `publish-docs.ps1` | Быстрая публикация (Windows) |
| `publish-docs.sh` | Быстрая публикация (Linux/macOS) |
| `.github/workflows/deploy-docs.yml` | Авто-публикация |
| `GITHUB_PUBLISH.md` | Полная инструкция |
| `QUICK_PUBLISH.md` | Шпаргалка |
| `PUBLISH_DOCS_GUIDE.md` | Руководство по публикации |

---

## 🔧 Если что-то пошло не так

### mdbook не найден
```bash
cargo install mdbook
```

### 404 страница
Подождите 1-2 минуты — GitHub обрабатывает публикацию.

### Ошибка при push
```bash
git push origin gh-pages --force
```

### Конфликт worktree
```bash
git worktree prune
```

---

## ✅ Чек-лист перед публикацией

- [ ] Все изменения закоммичены
- [ ] `git push origin main` выполнен
- [ ] mdbook установлен
- [ ] Документация собрана (`mdbook build`)
- [ ] Ветка gh-pages существует
- [ ] GitHub Pages настроен

---

## 🌐 Ссылки

- **Репозиторий:** https://github.com/krosovok52/media-sessions
- **Документация:** https://krosovok52.github.io/media-sessions/
- **Crates.io:** https://crates.io/crates/media-sessions
- **Docs.rs:** https://docs.rs/media-sessions

---

**Готово! 🎉**
