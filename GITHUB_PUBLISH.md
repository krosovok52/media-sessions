# 🚀 Полная инструкция по публикации на GitHub

Краткое руководство по публикации документации Media Sessions.

---

## ⚡ Самый быстрый способ

### Windows
```powershell
.\setup-and-publish.ps1
```

### Linux/macOS
```bash
./setup-and-publish.sh
```

**Всё!** Документация будет доступна по адресу:
```
https://krosovok52.github.io/media-sessions/
```

---

## 📋 Пошаговая инструкция

### Шаг 1: Подготовка

```bash
# Убедитесь, что вы в директории проекта
cd path/to/MediaSession

# Проверьте git статус
git status

# Закоммитьте все изменения
git add .
git commit -m "docs: подготовка к публикации"
```

### Шаг 2: Установка mdBook

```bash
cargo install mdbook
```

### Шаг 3: Проверка локально

```bash
cd docs
mdbook serve
# Открыть http://localhost:3000
```

### Шаг 4: Публикация

#### Вариант A: Автоматически (GitHub Actions)

```bash
# Просто запушьте в main
git push origin main

# GitHub Actions автоматически опубликует документацию
```

#### Вариант B: Скриптом

```bash
# Windows
.\setup-and-publish.ps1

# Linux/macOS
./setup-and-publish.sh
```

#### Вариант C: Вручную

```bash
# Собрать документацию
cd docs
mdbook build

# Опубликовать
git checkout --orphan gh-pages
git reset --hard
cp -r book/* .
touch .nojekyll
git add .
git commit -m "docs: publish documentation"
git push origin gh-pages --force
git checkout main
```

### Шаг 5: Настройка GitHub Pages (если нужно)

1. Перейдите в репозиторий на GitHub
2. **Settings** → **Pages**
3. **Source**: Deploy from a branch
4. **Branch**: gh-pages → **Save**

### Шаг 6: Проверка

Откройте в браузере:
```
https://krosovok52.github.io/media-sessions/
```

---

## 📁 Файлы для публикации

| Файл | Описание |
|------|----------|
| `publish-docs.sh` | Скрипт быстрой публикации (Linux/macOS) |
| `publish-docs.ps1` | Скрипт быстрой публикации (Windows) |
| `setup-and-publish.sh` | Полная настройка + публикация (Linux/macOS) |
| `setup-and-publish.ps1` | Полная настройка + публикация (Windows) |
| `.github/workflows/deploy-docs.yml` | GitHub Actions workflow |
| `PUBLISH_DOCS_GUIDE.md` | Полное руководство по публикации |
| `QUICK_PUBLISH.md` | Шпаргалка по публикации |

---

## 🔧 Troubleshooting

| Проблема | Решение |
|----------|---------|
| **mdbook не найден** | `cargo install mdbook` |
| **404 после публикации** | Подождать 1-2 минуты, GitHub обрабатывает |
| **Ошибка git push** | `git push origin gh-pages --force` |
| **Конфликт worktree** | `git worktree prune` |
| **Нет ветки gh-pages** | Скрипт создаст автоматически |

---

## 📊 Что происходит при публикации

```
1. Проверка зависимостей (git, cargo, mdbook)
         ↓
2. Сборка документации (mdbook build)
         ↓
3. Создание временной ветки
         ↓
4. Копирование файлов из docs/book/
         ↓
5. Коммит и push в ветку gh-pages
         ↓
6. Очистка временных файлов
         ↓
7. Готово! 🎉
```

---

## 🌐 URL документации

После публикации документация будет доступна по адресу:

```
https://krosovok52.github.io/media-sessions/
```

Или, если у репозитория другое имя:

```
https://<username>.github.io/<repo-name>/
```

---

## 🤖 GitHub Actions

Автоматическая публикация при:

- Push в ветку `main`
- Изменениях в `docs/`
- Ручном запуске из вкладки Actions

Workflow файл: `.github/workflows/deploy-docs.yml`

---

## 📚 Дополнительные ресурсы

- **PUBLISH_DOCS_GUIDE.md** — Полное руководство
- **QUICK_PUBLISH.md** — Шпаргалка
- **docs/README.md** — Документация по docs

---

## 📬 Контакты

| Платформа | Ссылка |
|-----------|--------|
| **GitHub Issues** | https://github.com/krosovok52/media-sessions/issues |
| **Telegram** | [@krosov_ok](https://t.me/krosov_ok) |

---

<div align="center">

**Версия:** 0.2.0 | **Последнее обновление:** Февраль 2026

*Успешной публикации! 🚀*

</div>
