# 📤 Инструкция по публикации документации

Полное руководство по публикации документации Media Sessions на GitHub Pages.

---

## 📋 Содержание

1. [Быстрый старт](#быстрый-старт)
2. [Автоматическая публикация (GitHub Actions)](#автоматическая-публикация-github-actions)
3. [Ручная публикация (скрипты)](#ручная-публикация-скрипты)
4. [Публикация через GitHub UI](#публикация-через-github-ui)
5. [Troubleshooting](#troubleshooting)

---

## 🚀 Быстрый старт

### Требования

| Требование | Версия | Как установить |
|------------|--------|----------------|
| **Git** | Любая | https://git-scm.com/ |
| **Rust** | 1.80+ | https://rustup.rs/ |
| **mdBook** | Любая | `cargo install mdbook` |

### 1. Установка mdBook

```bash
cargo install mdbook
```

### 2. Проверка установки

```bash
mdbook --version
```

### 3. Публикация (один скрипт)

#### Windows (PowerShell)

```powershell
# Из директории проекта
.\setup-and-publish.ps1
```

#### Linux/macOS (Bash)

```bash
# Из директории проекта
chmod +x setup-and-publish.sh
./setup-and-publish.sh
```

### 4. Результат

Документация будет доступна по адресу:
```
https://krosovok52.github.io/media-sessions/
```

---

## 🤖 Автоматическая публикация (GitHub Actions)

### Настройка

Workflow автоматически публикует документацию при:
- Push в ветку `main`
- Изменениях в `docs/` директории
- Ручном запуске из вкладки Actions

### Файл workflow

Создайте `.github/workflows/deploy-docs.yml`:

```yaml
name: Deploy Documentation

on:
  push:
    branches:
      - main
    paths:
      - 'docs/**'
      - 'book.toml'
  
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: dtolnay/rust-toolchain@stable
      
      - name: Install mdBook
        run: cargo install mdbook
      
      - name: Build documentation
        working-directory: docs
        run: mdbook build
      
      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./docs/book

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/deploy-pages@v4
```

### Запуск

1. Запушьте изменения в `main`:
   ```bash
   git add .
   git commit -m "docs: обновить документацию"
   git push origin main
   ```

2. Перейдите в **Actions** на GitHub
3. Дождитесь завершения workflow
4. Документация опубликована!

---

## 📝 Ручная публикация (скрипты)

### Скрипты

| Скрипт | Платформа | Описание |
|--------|-----------|----------|
| `publish-docs.sh` | Linux/macOS | Быстрая публикация |
| `publish-docs.ps1` | Windows | Быстрая публикация |
| `setup-and-publish.sh` | Linux/macOS | Полная настройка + публикация |
| `setup-and-publish.ps1` | Windows | Полная настройка + публикация |

### Использование

#### Windows

```powershell
# Быстрая публикация
.\publish-docs.ps1

# Полная настройка и публикация
.\setup-and-publish.ps1

# С автоматическим открытием браузера
.\setup-and-publish.ps1 -OpenBrowser
```

#### Linux/macOS

```bash
# Сделать скрипты исполняемыми
chmod +x publish-docs.sh
chmod +x setup-and-publish.sh

# Быстрая публикация
./publish-docs.sh

# Полная настройка и публикация
./setup-and-publish.sh
```

### Что делают скрипты

1. **Проверяют зависимости** (git, cargo, mdbook)
2. **Собирают документацию** (`mdbook build`)
3. **Создают ветку gh-pages** (если не существует)
4. **Копируют файлы** в временную директорию
5. **Публикуют на GitHub** (push в ветку gh-pages)
6. **Очищают** временные файлы

---

## 🖥️ Публикация через GitHub UI

### Шаг 1: Включить GitHub Pages

1. Перейдите в репозиторий на GitHub
2. **Settings** → **Pages**
3. **Source**: Deploy from a branch
4. **Branch**: gh-pages → Save

### Шаг 2: Создать ветку gh-pages

```bash
# Собрать документацию
cd docs
mdbook build

# Создать ветку
git checkout --orphan gh-pages
git reset --hard

# Копировать файлы
cp -r book/* .

# Добавить .nojekyll
touch .nojekyll

# Закоммитить
git add .
git commit -m "docs: initial documentation"

# Отправить
git push origin gh-pages
```

### Шаг 3: Проверить

Перейдите по адресу:
```
https://krosovok52.github.io/media-sessions/
```

---

## 🔧 Troubleshooting

### mdbook не найден

**Ошибка:**
```
❌ mdbook не найден
```

**Решение:**
```bash
cargo install mdbook
```

### git worktree ошибка

**Ошибка:**
```
fatal: invalid worktree
```

**Решение:**
```bash
# Удалить проблемные worktree
git worktree prune

# Попробовать снова
./publish-docs.sh
```

### Права доступа

**Ошибка:**
```
Permission denied (publickey)
```

**Решение:**
```bash
# Использовать HTTPS вместо SSH
git remote set-url origin https://github.com/YOUR_USERNAME/media-sessions.git

# Или настроить SSH ключ
ssh-keygen -t ed25519 -C "your_email@example.com"
```

### 404 после публикации

**Проблема:** Страница 404 после публикации

**Решение:**
1. Подождите 1-2 минуты (GitHub обрабатывает)
2. Проверьте настройки Pages: Settings → Pages
3. Убедитесь, что ветка gh-pages существует
4. Проверьте наличие `.nojekyll` файла

### Конфликты при пуше

**Ошибка:**
```
! [rejected] gh-pages -> gh-pages (non-fast-forward)
```

**Решение:**
```bash
# Использовать --force
git push origin gh-pages --force
```

### Сборка не работает

**Проблема:** `mdbook build` завершается с ошибкой

**Решение:**
```bash
# Проверить синтаксис markdown
cd docs
mdbook build --verbose

# Исправить ошибки в SUMMARY.md
# Проверить пути к файлам
```

---

## 📊 Структура после публикации

```
GitHub Repository
├── main ветка
│   ├── docs/
│   │   ├── src/          # Исходники документации
│   │   ├── book.toml     # Конфигурация
│   │   └── book/         # Сгенерированная документация
│   └── ...
│
└── gh-pages ветка (публикуется автоматически)
    ├── index.html        # Главная страница
    ├── css/              # Стили
    ├── js/               # Скрипты
    └── ...               # Другие файлы
```

---

## 🎯 Best Practices

### 1. Автоматизируйте

Используйте GitHub Actions для автоматической публикации:

```yaml
# .github/workflows/deploy-docs.yml
on:
  push:
    branches: [main]
    paths: ['docs/**']
```

### 2. Проверяйте перед публикацией

```bash
# Локальная проверка
cd docs
mdbook build
mdbook serve  # Открыть http://localhost:3000
```

### 3. Используйте .nojekyll

Создайте пустой файл `.nojekyll` в корне публикуемой документации:

```bash
touch docs/book/.nojekyll
```

Это отключает обработку Jekyll и ускоряет публикацию.

### 4. Следите за размером

Оптимизируйте изображения:

```bash
# Сжать изображения
find docs/src -name "*.png" -exec pngquant {} \;
```

---

## 📚 Дополнительные ресурсы

- **mdBook документация:** https://rust-lang.github.io/mdBook/
- **GitHub Pages документация:** https://docs.github.com/en/pages
- **GitHub Actions документация:** https://docs.github.com/en/actions

---

## 📬 Контакты

| Платформа | Ссылка |
|-----------|--------|
| **GitHub Issues** | https://github.com/krosovok52/media-sessions/issues |
| **Telegram** | [@krosov_ok](https://t.me/krosov_ok) |

---

<div align="center">

**Версия:** 0.2.0 | **Последнее обновление:** Февраль 2026

*Успешной публикации! 🚀*

</div>
